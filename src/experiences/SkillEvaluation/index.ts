/**
 * @deprecated Import from '@/experiences/EvaluationDetails' instead.
 * Temporary re-export shim — will be deleted after all imports are migrated.
 */
export {
  EvalScoreBadge,
  EvaluationDetails as SkillEvaluationDetails,
  EvalScoreBar,
  EvalAssertionList,
  EvalRadarChart,
} from '../EvaluationDetails';
